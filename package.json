{
  "name": "nzip",
  "version": "1.13.0",
  "type": "module",
  "scripts": {
    "start": "bun Main.ts",
    "dev": "bun Main.ts",
    "prebuild": "bun Scripts/pre-build.ts",
    "build": "bun Scripts/build.ts",
    "postbuild": "bun Scripts/post-build.ts",
    "build:core": "bun Scripts/build-core.ts",
    "build:docker": "bun Scripts/build-docker.ts",
    "dev:docker": "docker compose -f compose.dev.yml up --build",
    "prettier": "prettier --write ."
  },
  "dependencies": {
    "@icebrick/nhget": "^2.0.1",
    "bun": "^1.3.11",
    "cron": "^4.4.0",
    "hono": "^4.12.9"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "prettier": "^3.8.1"
  }
}
